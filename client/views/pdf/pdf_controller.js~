this.PdfController = RouteController.extend({
	template: "pdf",

	yieldTemplate: {
	  /*YIELD_TEMPLATES*/

	},
		action: function () {

	            this.render('pdf');

	},

		witeOne: function () {
	//  return.Meteor.subscribe('pdfdetail', this.params.id);
	}
		});
